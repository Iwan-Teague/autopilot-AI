{
  "name": "Rustynet CLI Phases 4-7",
  "spec_source": "/Users/iwan/Desktop/Rustynet/documents/CliCommandsDesign.md",
  "working_directory": "/Users/iwan/Desktop/Rustynet/.claude/worktrees/charming-swartz-ddef31",
  "global_rules": [
    "All commands pull real live OS data via rustynet-sysinfo — zero stubs, zero placeholders",
    "Cross-platform: macOS, Windows, Debian 11+; use cfg(target_os) patterns; no runtime failures",
    "No TODO/FIXME in completed deliverables; all functions fully wired; no deferred work",
    "Default human-readable output, --json flag on all commands, ISO 8601 timestamps, consistent numeric units"
  ],
  "interface": "webhook",
  "provider": "auto",
  "auto_paste": true,
  "target_app": "Claude",
  "port": 7432,
  "branch": "charming-swartz-ddef31",
  "branch_pinned": true,
  "state_path": "autopilot-state.json",
  "stages": [
    {
      "id": "foundation",
      "phase": "foundation",
      "model": "claude-haiku-4-5",
      "summary": "Review CLI structure, plan enum + command additions for Phases 4-7",
      "prompt": "## Context\nFoundation stage for Rustynet CLI Phases 4-7 implementation. You are in /Users/iwan/Desktop/Rustynet/.claude/worktrees/charming-swartz-ddef31/ (a git worktree of the main Rustynet project).\n\nPhases 1-3 (21 commands) are complete. You will implement Phases 4-7 (18 new commands across 7 categories):\n- Phase 4: Node management (info, list, probe)\n- Phase 5: Policy + Certs/Trust (policy list/test/apply, cert list/check, trust-state)\n- Phase 6: Relay + Analytics (relay list/select/health, analytics peers/traffic/latency-heatmap)\n- Phase 7: Backup/Restore + Config (backup state/restore state/export-keys, config show/validate/export)\n\n## Objective\nReview the existing CLI structure and plan the enum/command additions:\n\n1. Read `crates/rustynet-cli/src/main.rs` — identify the CliCommand enum, parse_command(), and main match statement\n2. Review `crates/rustynet-sysinfo/src/lib.rs` — understand available data collection functions for each category\n3. Create a structured implementation plan:\n   - List all 18 new command variants to add to CliCommand enum\n   - Group by category (8 groups) with brief notes on data sources from rustynet-sysinfo\n   - Identify any new rustynet-sysinfo functions needed (document if any are missing)\n   - Note CLI argument patterns (flags, required vs optional args)\n4. Document this plan in `planning/cli-phases-4-7-plan.md` (create the planning/ dir if needed)\n\n## Acceptance criteria\n- File `crates/rustynet-cli/src/main.rs` reviewed and summarized\n- File `crates/rustynet-sysinfo/src/lib.rs` reviewed and summarized\n- File `planning/cli-phases-4-7-plan.md` created with:\n  - All 18 command variants listed by category\n  - Data sources identified for each\n  - Any missing rustynet-sysinfo functions flagged\n  - CLI argument patterns documented\n- Plan is clear enough that implementation stages can build on it\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"foundation\",\"summary\":\"CLI structure reviewed, implementation plan created in planning/cli-phases-4-7-plan.md.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    },
    {
      "id": "phase4-nodes",
      "phase": "implementation",
      "model": "claude-opus-4-7",
      "summary": "Implement node management commands: node info, node list, node probe",
      "prompt": "## Context\nPhase 4 implementation. Foundation stage completed — CLI structure reviewed and plan exists in `planning/cli-phases-4-7-plan.md`.\n\nYou are implementing **Category 8: Node and Membership Management** from the spec (section 8.1-8.3):\n- `rustynet node info [--peers] [--json]` — Display node info (ID, public key, role, join date, last heartbeat, peer count, trust state freshness)\n- `rustynet node list [--role <relay|exit|all>] [--filter <status>] [--json]` — List all known nodes with optional filtering\n- `rustynet node probe <node-id> [--tcp-port <port>] [--udp-port <port>] [--json]` — Probe reachability to specific node\n\n## Objective\nImplement the 3 node management commands in `crates/rustynet-cli/src/main.rs`:\n\n1. Add enum variants to CliCommand for each command (follow existing pattern)\n2. Extend parse_command() to parse the 3 new commands with their flags\n3. Wire each command into the main match statement with handler functions\n4. Implement handler functions:\n   - Query membership state + node metadata from daemon or config\n   - Call appropriate rustynet-sysinfo functions for network data (latency, reachability)\n   - Format output (human-readable table + --json)\n   - Graceful error handling for permission denied or data unavailable\n5. All output fields from spec must be present (non-negotiable)\n6. Cross-platform: ensure calls work on macOS, Windows, Debian\n\nOutput a unified diff or targeted file edits only — do not paste full file contents.\n\n## Acceptance criteria\n- All 3 node commands fully wired in main.rs (no TODO/FIXME)\n- Commands parse all required and optional flags correctly\n- Output includes all fields from spec (node ID, public key, role, latency, endpoint, status, etc.)\n- --json flag produces valid JSON for each command\n- No cross-platform runtime errors (use cfg patterns if needed)\n- `cargo check --workspace --all-targets --all-features` passes\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"phase4-nodes\",\"summary\":\"Node management commands (info, list, probe) fully implemented and wired in CLI.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    },
    {
      "id": "phase5a-policy",
      "phase": "implementation",
      "model": "claude-opus-4-7",
      "summary": "Implement policy commands: list, test, apply",
      "prompt": "## Context\nPhase 5a implementation. Phases 1-4 complete. You are implementing **Category 9: Policy and Authorization** (spec sections 9.1-9.3):\n- `rustynet policy list [--node <id>] [--json]` — Display active policies and allow rules\n- `rustynet policy test <source-node> <dest-node> [--protocol tcp|udp] [--port <n>] [--json]` — Test if a flow is allowed\n- `rustynet policy apply <policy-file> [--dry-run] [--json]` — Apply or preview policy config\n\n## Objective\nImplement the 3 policy commands in `crates/rustynet-cli/src/main.rs`:\n\n1. Add enum variants to CliCommand for each policy command\n2. Extend parse_command() to handle policy subcommands and their flags\n3. Wire into main match statement with handler functions\n4. Implement handlers:\n   - policy list: Query policy-engine state from daemon, parse PolicySet + PolicyRule\n   - policy test: Simulate policy evaluation, return first matching rule and allow/deny decision\n   - policy apply: Compare policy-file against current state, show changes (added/modified/removed), validate syntax\n   - All handlers support --json output\n5. Cross-platform implementation (macOS, Windows, Debian)\n\nOutput a unified diff or targeted file edits only — do not paste full file contents.\n\n## Acceptance criteria\n- All 3 policy commands fully wired (no TODO/FIXME)\n- Flags parsed correctly (--node, --dry-run, --protocol, --port)\n- policy test returns allow/deny + matching rule\n- policy apply validates rule syntax and detects conflicts\n- --json output valid for all commands\n- `cargo check --workspace --all-targets --all-features` passes\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"phase5a-policy\",\"summary\":\"Policy commands (list, test, apply) fully implemented in CLI.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    },
    {
      "id": "phase5b-certs",
      "phase": "implementation",
      "model": "claude-sonnet-4-6",
      "summary": "Implement cert + trust commands: list, check, trust-state",
      "prompt": "## Context\nPhase 5b implementation. Phases 1-5a complete. You are implementing **Category 11: Certificate and Trust Management** (spec sections 11.1-11.3):\n- `rustynet cert list [--expired] [--expiring-soon] [--json]` — List certificates with expiry info\n- `rustynet cert check [--strict] [--json]` — Comprehensive cert validation and chain verification\n- `rustynet trust-state show [--anchor <node-id>] [--json]` — Display signed trust state artifact\n\n## Objective\nImplement the 3 cert/trust commands in `crates/rustynet-cli/src/main.rs`:\n\n1. Add enum variants to CliCommand\n2. Extend parse_command() for cert subcommands and flags\n3. Wire into main match with handler functions\n4. Implement handlers:\n   - cert list: Scan config PKI + systemd certs, parse PEM/DER, show issued/expires/days-remaining\n   - cert check: Call cert validation from crypto crate, verify signatures, check revocation if CRL available\n   - trust-state show: Parse signed trust-state artifact, show version, timestamp, signer, freshness, member count\n   - All handlers support --json\n5. Cross-platform (macOS, Windows, Debian)\n\nOutput a unified diff or targeted file edits only — do not paste full file contents.\n\n## Acceptance criteria\n- All 3 cert/trust commands fully wired (no TODO/FIXME)\n- Flags parsed (--expired, --expiring-soon, --strict, --anchor)\n- cert list shows all fields: subject, issuer, serial, issued, expires, days-remaining, type, key-size\n- cert check validates chain and shows any warnings\n- trust-state shows freshness (current/stale) and age in seconds\n- --json valid for all commands\n- `cargo check --workspace --all-targets --all-features` passes\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"phase5b-certs\",\"summary\":\"Certificate and trust-state commands (list, check, trust-state show) implemented in CLI.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    },
    {
      "id": "phase6a-relay",
      "phase": "implementation",
      "model": "claude-opus-4-7",
      "summary": "Implement relay commands: list, select, health",
      "prompt": "## Context\nPhase 6a implementation. Phases 1-5b complete. You are implementing **Category 10: Relay and Exit Node Management** (spec sections 10.1-10.3):\n- `rustynet relay list [--status] [--json]` — List relay servers and status\n- `rustynet relay select [--best-latency] [--least-load] [--json]` — Query/override relay selection strategy\n- `rustynet relay health <relay-id> [--json]` — Detailed health check of specific relay\n\n## Objective\nImplement the 3 relay commands in `crates/rustynet-cli/src/main.rs`:\n\n1. Add enum variants to CliCommand\n2. Extend parse_command() for relay subcommands and flags\n3. Wire into main match with handler functions\n4. Implement handlers:\n   - relay list: Query relay store + endpoint hints, poll relay health checks, show load (active connections), uptime, geo location\n   - relay select: Show current relay selection strategy (manual/auto/latency/load), rank alternatives by metric\n   - relay health: Live probe or cached health stats, show RTT, packet loss, last probe timestamp, consecutive failures, uptime % trend\n   - All handlers support --json\n5. Cross-platform (macOS, Windows, Debian)\n\nOutput a unified diff or targeted file edits only — do not paste full file contents.\n\n## Acceptance criteria\n- All 3 relay commands fully wired (no TODO/FIXME)\n- Flags parsed (--status, --best-latency, --least-load)\n- relay list shows: relay ID/hostname, role (relay/exit), load, uptime, geo location\n- relay select shows ranking and selection criteria\n- relay health shows RTT (ms), packet loss (%), consecutive failures, uptime % trend\n- --json valid for all commands\n- `cargo check --workspace --all-targets --all-features` passes\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"phase6a-relay\",\"summary\":\"Relay commands (list, select, health) fully implemented in CLI.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    },
    {
      "id": "phase6b-analytics",
      "phase": "implementation",
      "model": "claude-opus-4-7",
      "summary": "Implement analytics commands: peers, traffic, latency-heatmap",
      "prompt": "## Context\nPhase 6b implementation. Phases 1-6a complete. You are implementing **Category 12: Analytics and Metrics** (spec sections 12.1-12.3):\n- `rustynet analytics peers [--window <duration>] [--sort <metric>] [--json]` — Peer health trends and latency heatmap\n- `rustynet analytics traffic [--interval <duration>] [--top <n>] [--json]` — Traffic summary by peer/node/protocol\n- `rustynet analytics latency-heatmap [--peers] [--relays] [--json]` — Latency matrix between this node and all peers/relays\n\n## Objective\nImplement the 3 analytics commands in `crates/rustynet-cli/src/main.rs`:\n\n1. Add enum variants to CliCommand\n2. Extend parse_command() for analytics subcommands and flags\n3. Wire into main match with handler functions\n4. Implement handlers:\n   - analytics peers: Collect historical metrics (or recent samples), compute percentiles/trends, show avg latency, uptime %, data transferred, availability sparkline\n   - analytics traffic: Aggregate netlink or packet-level data by 5-min intervals, report top-N by throughput, show bytes/packets/duration\n   - analytics latency-heatmap: Generate latency matrix (rows=destinations, cols=percentiles p50/p95/p99), ASCII color-coded + JSON grid\n   - All handlers support --json\n5. Cross-platform (macOS, Windows, Debian)\n\nOutput a unified diff or targeted file edits only — do not paste full file contents.\n\n## Acceptance criteria\n- All 3 analytics commands fully wired (no TODO/FIXME)\n- Flags parsed (--window, --sort, --interval, --top, --peers, --relays)\n- analytics peers shows: peer ID, avg/min/max/stddev latency, uptime %, data transferred, sparkline\n- analytics traffic shows: source→destination, protocol, bytes/packets, throughput (Mbps)\n- analytics latency-heatmap produces valid matrix with p50/p95/p99 columns\n- --json valid for all commands\n- `cargo check --workspace --all-targets --all-features` passes\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"phase6b-analytics\",\"summary\":\"Analytics commands (peers, traffic, latency-heatmap) fully implemented in CLI.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    },
    {
      "id": "phase7a-backup",
      "phase": "implementation",
      "model": "claude-sonnet-4-6",
      "summary": "Implement backup/restore commands: state, restore, export-keys",
      "prompt": "## Context\nPhase 7a implementation. Phases 1-6b complete. You are implementing **Category 13: Backup and Recovery** (spec sections 13.1-13.3):\n- `rustynet backup state [--path <dir>] [--compress] [--encrypt] [--json]` — Snapshot and backup node state\n- `rustynet restore state [--path <backup-file>] [--verify] [--dry-run] [--json]` — Restore node state from backup\n- `rustynet export-keys [--format <pem|raw>] [--path <file>] [--json]` — Export node private keys (with warnings)\n\n## Objective\nImplement the 3 backup/restore commands in `crates/rustynet-cli/src/main.rs`:\n\n1. Add enum variants to CliCommand\n2. Extend parse_command() for backup subcommands and flags\n3. Wire into main match with handler functions\n4. Implement handlers:\n   - backup state: Tar/gzip config dirs + daemon state, optionally encrypt with node key, show backup location/size/items/encryption status\n   - restore state: Unpack tarball, verify signatures, prompt on conflicts, show restoration result + checksum verification\n   - export-keys: Prompt for passphrase, export to PEM or raw format, DANGER warning, recommend secure storage\n   - All handlers support --json\n5. Cross-platform (macOS, Windows, Debian)\n\nOutput a unified diff or targeted file edits only — do not paste full file contents.\n\n## Acceptance criteria\n- All 3 backup/restore commands fully wired (no TODO/FIXME)\n- Flags parsed (--path, --compress, --encrypt, --verify, --dry-run, --format)\n- backup state creates tarball with keys, config, certs, metadata\n- restore state unpacks and verifies checksums\n- export-keys shows DANGER warning and file permissions recommendation\n- --json valid for all commands\n- `cargo check --workspace --all-targets --all-features` passes\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"phase7a-backup\",\"summary\":\"Backup/restore commands (backup state, restore state, export-keys) fully implemented in CLI.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    },
    {
      "id": "phase7b-config",
      "phase": "implementation",
      "model": "claude-haiku-4-5",
      "summary": "Implement config commands: show, validate, export",
      "prompt": "## Context\nPhase 7b implementation. Phases 1-7a complete. You are implementing **Category 14: Configuration Management** (spec sections 14.1-14.3):\n- `rustynet config show [--section <name>] [--json]` — Display current configuration with safe defaults visible\n- `rustynet config validate [--strict] [--json]` — Validate configuration syntax and semantic constraints\n- `rustynet config export [--format <toml|json|yaml>] [--path <file>] [--json]` — Export current configuration in portable format\n\n## Objective\nImplement the 3 config commands in `crates/rustynet-cli/src/main.rs`:\n\n1. Add enum variants to CliCommand\n2. Extend parse_command() for config subcommands and flags\n3. Wire into main match with handler functions\n4. Implement handlers:\n   - config show: Pretty-print daemon config or config file, highlight defaults vs overrides, redact sensitive fields\n   - config validate: Check required fields, type correctness, range constraints, report violations and fix suggestions\n   - config export: Serialize current config to requested format (TOML/JSON/YAML), redact sensitive fields, write to file if --path provided\n   - All handlers support --json\n5. Cross-platform (macOS, Windows, Debian)\n\nOutput a unified diff or targeted file edits only — do not paste full file contents.\n\n## Acceptance criteria\n- All 3 config commands fully wired (no TODO/FIXME)\n- Flags parsed (--section, --strict, --format, --path)\n- config show displays all sections with defaults highlighted\n- config validate detects constraint violations (CIDR overlap, port range, etc.)\n- config export supports toml, json, yaml formats\n- Sensitive fields (keys, passwords) are redacted in all outputs\n- --json valid for all commands\n- `cargo check --workspace --all-targets --all-features` passes\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"phase7b-config\",\"summary\":\"Config commands (show, validate, export) fully implemented in CLI.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    },
    {
      "id": "test-unit",
      "phase": "testing",
      "model": "claude-haiku-4-5",
      "summary": "Unit + integration tests for all 18 new commands",
      "prompt": "## Context\nTesting phase. All 18 commands (Phases 4-7) fully implemented and wired. Now write unit and integration tests.\n\n## Objective\nAdd comprehensive tests for all 18 new CLI commands in `crates/rustynet-cli/src/`:\n\n1. Create test module(s) for each category:\n   - `mod tests { mod node_commands { ... } }`\n   - `mod policy_commands { ... }`\n   - `mod cert_commands { ... }`\n   - `mod relay_commands { ... }`\n   - `mod analytics_commands { ... }`\n   - `mod backup_commands { ... }`\n   - `mod config_commands { ... }`\n\n2. For each command, write tests:\n   - Parsing test: verify parse_command() correctly parses flags and arguments\n   - Output format test: verify human-readable and --json outputs are valid\n   - Flag test: verify optional flags (e.g., --peers, --strict, --format) work correctly\n   - Error handling test: verify graceful fallback for permission denied or missing data\n\n3. Sample test structure:\n   ```rust\n   #[test]\n   fn test_node_info_parse() {\n     let cmd = parse_command(vec![\"node\", \"info\", \"--peers\", \"--json\"]);\n     assert!(matches!(cmd, CliCommand::NodeInfo { peers: true, json: true }));\n   }\n   ```\n\n4. Run all tests:\n   ```bash\n   cargo test --workspace --all-targets --all-features\n   ```\n\n## Acceptance criteria\n- At least 3 tests per command category (21+ tests total)\n- All tests pass: `cargo test --workspace --all-targets --all-features`\n- Tests cover: parsing, output formats, flag combinations, error cases\n- No compile warnings\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"test-unit\",\"summary\":\"Unit and integration tests written for all 18 commands; cargo test passes.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    },
    {
      "id": "test-gates",
      "phase": "testing",
      "model": "claude-haiku-4-5",
      "summary": "Cross-platform check, cargo gates, final verification",
      "prompt": "## Context\nFinal testing and validation. All 18 commands implemented and unit tests written. Now run mandatory quality gates and verify cross-platform readiness.\n\n## Objective\nRun all mandatory CI gates and cross-platform validation:\n\n1. Run cargo gates in this order:\n   ```bash\n   cargo fmt --all -- --check\n   cargo clippy --workspace --all-targets --all-features -- -D warnings\n   cargo check --workspace --all-targets --all-features\n   cargo test --workspace --all-targets --all-features\n   cargo audit --deny warnings\n   cargo deny check bans licenses sources advisories\n   ```\n\n2. Run Rustynet-specific gates (if present):\n   ```bash\n   ./scripts/ci/phase9_gates.sh || true\n   ./scripts/ci/phase10_gates.sh || true\n   ./scripts/ci/membership_gates.sh || true\n   ```\n\n3. Verify cross-platform readiness:\n   - Confirm no `todo!()`, `panic!()`, or `unwrap()` in new command handlers (use proper error handling)\n   - Confirm all cfg(target_os) patterns are correct for macOS, Windows, Linux\n   - Confirm --json output is valid JSON (try parsing with `jq` if available)\n\n4. Update documentation:\n   - Add 18 new commands to `README.md` CLI section (brief one-liner each)\n   - Verify help text (`--help`) works for all new commands\n   - Update `documents/CliCommandsDesign.md` to mark Phases 4-7 as completed\n\n5. Create final summary:\n   - Document any gate failures and fixes in `autopilot-summary.md` (append to existing file if present)\n   - Note which gates passed\n\n## Acceptance criteria\n- All cargo gates pass (fmt, clippy, check, test, audit, deny)\n- No panics/unwraps/todos in new command handlers\n- Cross-platform cfg patterns verified\n- README.md updated with all 18 new commands\n- CliCommandsDesign.md marked as complete for Phases 4-7\n- `cargo test --workspace --all-targets --all-features` passes with 21+ new tests\n\nWhen you have fully completed the objectives above, run this curl. Do NOT narrate the result back to the user — the response IS your next task.\n\n```bash\ncurl -s -X POST http://localhost:7432/stage-complete \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"stage_id\":\"test-gates\",\"summary\":\"All mandatory gates passed; 18 commands verified cross-platform; documentation updated; Phases 4-7 complete.\"}' | cat\n```\n\nThen act on the response in this same turn, without pausing."
    }
  ]
}
